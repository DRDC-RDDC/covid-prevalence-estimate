apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: repo-data
  annotations:
    volume.beta.kubernetes.io/storage-class: aws-gp2-enc
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: aws-gp2-enc
  resources:
    requests:
      storage: 30Gi